{
    "routes": [
        {
            "src": "/api/(.*)",
            "headers": {
                "cache-control": "s-maxage=60"
            },
            "dest": "/api/$1",
            "continue": true
        }
    ]
}